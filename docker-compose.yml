version: '3.8'
services:
  mongodb:
    image: mongo:7
    container_name: diagnosis-mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ['CMD', 'mongosh', '--eval', 'db.runCommand("ping").ok']
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: diagnosis-app
    ports:
      - '8080:8080'
    env_file: .env
    environment:
      MONGO_URI: mongodb://mongodb:27017/diagnosisdb
    depends_on:
      mongodb:
        condition: service_healthy

volumes:
  mongo_data:
